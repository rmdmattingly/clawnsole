{
  "name": "clawnsole",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive UI for OpenClaw live thinking and activity",
  "scripts": {
    "dev": "node server.js",
    "dev:prod": "CLAWNSOLE_INSTANCE=prod PORT=5173 node server.js",
    "dev:qa": "CLAWNSOLE_INSTANCE=qa PORT=5174 node server.js",
    "serve": "node server.js",
    "test": "npm run test:syntax && npm run test:unit && npm run test:proxy",
    "test:syntax": "node --check gateway-client.js && node --check app.js && node --check proxy.js && node --check clawnsole-server.js && node --check server.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:proxy": "node scripts/proxy-self-test.js",
    "test:smoke": "node scripts/smoke-test.js",
    "test:ui": "playwright test --workers=1",
    "test:deploy": "playwright test tests/deploy.e2e.spec.js"
  },
  "bin": {
    "clawnsole": "bin/clawnsole.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
